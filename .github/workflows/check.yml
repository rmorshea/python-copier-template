---
name: check
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
env:
  latest-python-version: 3.13
jobs:
  samples:
    runs-on: ubuntu-latest
    steps:
      - name: Check basic
        uses: ./samples/basic/.github/workflows/check.yml
